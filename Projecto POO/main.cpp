#include <iostream>
#include <string>
#include <vector>
#include <time.h>
#include <sstream>
#include "Consola.h"
#include "Terreno.h"
#include "Faccoes.h"
#include "Habitantes.h"
#include "Artefactos.h"
#include "Jogo.h"
#include "Menu.h"
using namespace std;


void main(void) {

//INICIO DEMO 
	/*
	vector <string> nomefac;
	nomefac.push_back("Espanhois");
	nomefac.push_back("Portugueses");
	nomefac.push_back("CPU1");
	nomefac.push_back("CPU2");
	jogo novojogo(4,nomefac); 
	novojogo.jogar(4,7,7); 
*/	
	//INICIO DEMO

menu Iniciar;
	Iniciar;

}









	//substituir este pedaço de codigo por uma função da classe faccoes(?) ou jogo(?)
/*	string nome;
	for(int i=0; i<n_faccoes;i++){
		cout << "Qual o nome da " << i+1 << "a faccao?" << endl;
		cin >> nome;
		f[i].setNome(nome);
		f[i].setOuro(20); //substituir 20 por uma variavel pois o valor varia consoante a dificuldade
		f[i].setComida(20); //substituir 20 por uma variavel pois o valor varia consoante a dificuldade
		if(i<n_jogadores) //as primeiras i faccoes, sao jogadores
			f[i].setJogador(1); //é jogador
		else
			f[i].setJogador(0); //é pc
		//criar habitante base (campones?)
		f[i].setCor(i+1);
		f[i].setHabitantes(1); // habitantes inicializados a 1

		//limpar ecran
		c.clrscr();
	}*/

	//Pedir dificuldade ao utilizador
	//==